# =============================================================================
# VALENTINE RF — Jetson Orin Nano (aarch64) Python Dependencies
# =============================================================================
# Target: NVIDIA Jetson Orin Nano / Ubuntu L4T / aarch64
# Python: 3.10+ (system-managed, installed into project venv only)
#
# IMPORTANT:
#   - All packages verified to have aarch64 wheels or build cleanly from source
#   - numpy/scipy versions chosen for aarch64 wheel availability
#   - No CUDA/cuDNN packages — Jetson provides these at the system level
#   - Install with: pip install --no-cache-dir -r requirements-jetson.txt
# =============================================================================

# ---------------------------------------------------------------------------
# CORE (Required — the application will not start without these)
# ---------------------------------------------------------------------------
quart==0.20.0                    # Async web framework (Flask replacement)
quart-rate-limiter==0.11.0       # Rate limiting middleware
httpx==0.28.1                    # Async HTTP client
Werkzeug==3.1.3                  # WSGI utilities (password hashing, routing)

# ---------------------------------------------------------------------------
# OPTIONAL — Signal Processing & Image Decoding
# ---------------------------------------------------------------------------
# numpy/scipy: aarch64 wheels are available from PyPI for these versions.
# If pip compilation fails, install system packages first:
#   sudo apt-get install python3-numpy python3-scipy
# then re-run pip with --system-site-packages on the venv.
numpy>=1.24.0,<3.0               # Numerical arrays (SSTV, DSC, waterfall FFT)
scipy>=1.10.0,<2.0               # Signal processing (DSC decoder, filters)
Pillow>=9.0.0,<12.0              # Image manipulation (SSTV output)

# ---------------------------------------------------------------------------
# OPTIONAL — Satellite Tracking
# ---------------------------------------------------------------------------
skyfield>=1.45,<2.0              # Orbital mechanics, TLE pass prediction

# ---------------------------------------------------------------------------
# OPTIONAL — Hardware & Peripherals
# ---------------------------------------------------------------------------
pyserial==3.5                    # USB GPS receiver support
bleak>=0.21.0,<1.0               # Bluetooth LE scanning (cross-platform)
websocket-client>=1.6.0,<2.0    # KiwiSDR WebSocket proxy

# ---------------------------------------------------------------------------
# OPTIONAL — Mesh Networking
# ---------------------------------------------------------------------------
meshtastic>=2.0.0,<3.0           # Meshtastic LoRa mesh networking

# ---------------------------------------------------------------------------
# OPTIONAL — Security / TSCM
# ---------------------------------------------------------------------------
scapy>=2.4.5,<3.0                # WiFi deauthentication detection

# ---------------------------------------------------------------------------
# OPTIONAL — Database
# ---------------------------------------------------------------------------
# psycopg2-binary may not have aarch64 wheels. If it fails, the installer
# will fall back to building psycopg2 from source (requires libpq-dev).
psycopg2-binary>=2.9.9,<3.0     # PostgreSQL driver (ADS-B history)

# ---------------------------------------------------------------------------
# OPTIONAL — Utilities
# ---------------------------------------------------------------------------
qrcode[pil]>=7.4,<9.0           # QR code generation (Meshtastic channels)
