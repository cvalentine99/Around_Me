# Development dependencies
-r requirements.txt

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.15.1

# Code quality (ruff replaces black for formatting + linting)
ruff>=0.9.0
mypy>=1.0.0

# Type stubs
types-Werkzeug>=1.0.0
